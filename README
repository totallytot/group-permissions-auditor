Groups Permissions Audit plugin represents Confluence scheduled job with GUI.
Plugin page location: cog icon -> General Configuration -> Group Permissions Audit
Confluence scheduled job can be managed from the plugin GUI directly.
Features:
 - configurable parameters for the job;
 - notifications;
 - report in UI;
 - group permissions removal.